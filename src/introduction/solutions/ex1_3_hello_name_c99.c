/*
 * ファイル名: ex1_3_hello_name_c99.c
 * 演習1-3: 基本的なプログラム作成
 * 説明: ユーザーに名前の入力を求め、挨拶メッセージを表示する
 * 規格: C99準拠（//コメント、変数宣言の位置の自由化）
 */

#include <stdio.h>

int main(void)
{
    // 名前の入力を求める
    printf("あなたの名前を入力してください: ");
    
    // 名前を格納する配列（使用箇所で宣言）
    char name[100];
    
    // 標準入力から名前を読み込み
    if (scanf("%99s", name) == 1)
    {
        // 挨拶メッセージを表示
        printf("こんにちは、%sさん!\n", name);
    }
    else
    {
        // 入力エラーの場合
        printf("入力エラーが発生しました。\n");
        return 1;
    }

    return 0;
}
